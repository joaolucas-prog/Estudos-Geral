package br.com.ingrao.estudoutil.modelo;

public class Aula5 {
	/* Aprendemos um pouco sobre sort na lista , mas esse metodo foi implementado a partir da versão 8 do java e como faziam isso
	 * antes ? Existe uma classe chamada Collections e nela possui varios metodos prontos para você usar
	 * 
	 * Alguns metodos da Collections
	 * .sort(List<T> l , Comparable<T> c) ordena a lista baseado no comparable
	 * .reverse() ordena a lista do maior para o menos
	 * 
	 * Ordem Natural
	 * Podemos fazer com que a própria classe defina qual será o criterio de comparação dela com outra classe, para isso
	 * você implementa a interface Comparable , não confudir com Comparator, e implementar o metodo compareTo
	 * E para comparar usando a ordem natural você deve passar como paramentro o valor null]
	 * 
	 * lista.sort(null);*/

}
